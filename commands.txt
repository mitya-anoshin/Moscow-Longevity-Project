pip install fastapi[all]

pip install sqlalchemy alembic psycopg2

alembic init migrations

alembic revision --autogenerate -m "db create"

alembic upgrade 70e5741a5e75

https://fastapi-users.github.io/fastapi-users/11.0/installation/


Column("id", Integer, primary_key=True),
    Column("registered_at", TIMESTAMP, default=datetime.utcnow),
    Column('gender', String, nullable=False),
    Column("born_at", TIMESTAMP, default=datetime.utcnow),
    Column('street', String, nullable=False)